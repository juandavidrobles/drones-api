export enum ExceptionValue {
  DRONE_BATTERY_TOO_LOW = 'Drone battery too low for loading',
  DRONE_NOT_LOADABLE = 'Drone not in a loadable state',
  DRONE_WEIGHT_EXCEEDED = 'Loading this medication would exceed drone weight limit',
}
